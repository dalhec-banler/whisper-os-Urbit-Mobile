# 2026-06-22 ROM 2026062201 Verification

Device: husky / Pixel 8 Pro

## Summary

Signed ROM `2026062201` was generated, flashed with a no-wipe fastboot update,
and verified on device. This release bakes the Urbit-side hosted-app safety fix
and the rebuilt Satellite Pill v1 artifact.

## Release Artifacts

- `husky-factory-2026062201.zip`
- `husky-img-2026062201.zip`
- `husky-install-2026062201.zip`
- `husky-ota_update-2026062201.zip`
- `husky-target_files.zip`

## Baked Verification

- Target files report `ro.build.version.incremental=2026062201`.
- Target files include `NativePlanetController.apk` with the local Eyre origin
  and `%nativeplanet-mobile` hosted-app polling logic.
- Target files include Satellite Pill v1 SHA256:

```text
0e6e61a0362180c3954ac8dd8607c132dcbf2a92fc63a6113ac0007163fd204e
```

## Flash Verification

- No-wipe fastboot update completed successfully.
- Device booted with `sys.boot_completed=1`.
- Active slot after flash: `_b`.
- Build after flash: `2026062201`.
- Build type: `userdebug`.
- `sys.init.updatable_crashing` is empty.
- Existing moon data survived the update.
- WiFi remained connected and validated.

## Runtime Verification

- `NativePlanetController` is running.
- Vere king and serf processes are running.
- Device pill hash matches the baked Satellite Pill v1 hash.
- Provider `/runtime` reports:
  - `state=running`
  - `version=4.3-33293b1`
  - `lastError=null`
  - `connSockAvailable=true`
- Provider `/status` reports:
  - `controllerAvailable=true`
  - `bootPackage.valid=true`
  - `bootPackage.bootMode=MOON`
  - `network.networkType=WIFI`
  - `network.validated=true`
  - `network.resolverAvailable=true`

## Hosted Apps Verification

- Hosted-app provider reports `source=docket+nativeplanet-mobile`.
- `mobileMetadataAvailable=true`.
- `lastError=null`.
- `stale=false`.
- Five apps are present: Grove, Kin, Landscape, Terminal, and Tlon.
- All five are inventory-only entries with no local launch action exposed while
  the local Eyre routes remain unavailable.

## Smoke Tests

- `smoke-controller-provider.sh`: pass.
- `smoke-hosted-mobile-apps.sh`: pass.
- `smoke-launcher-ui.sh`: pass.

## Crash And Policy Sweep

- No NativePlanet or Launcher fatal exceptions were found after flash.
- No relevant `nativeplanet`, `vere`, `launcher`, or `system_app` AVC denials
  were found after flash.

## Product State

The phone now has a truthful Urbit app inventory baked into the ROM. Broken
local Eyre routes are not exposed as Open actions. The remaining Urbit-side
product work is to create real mobile app surfaces for Tlon, Terminal, Grove,
Kin, and future parent-managed mobile apps.
