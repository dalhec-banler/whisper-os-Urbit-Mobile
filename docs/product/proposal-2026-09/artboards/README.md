Artboards for the 2026-09 launcher proposal (`../nouns-before-apps.html`); `earlier/` holds the superseded directions and the canvas layout.
The `.dc.html` files render inside the design canvas host, not standalone.
`./support.js`, which each artboard references, is provided by that host and is not checked in.
